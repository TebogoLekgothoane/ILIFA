# Server-side only. Do not put this key in the Expo app.
# The API server also reads env.local in this folder.

GEMINI_API_KEY=
PORT=5000

# Optional: point the Expo app at the backend. This is not a secret.
# EXPO_PUBLIC_API_URL=http://localhost:5000
